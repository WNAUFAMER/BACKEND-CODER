<div>
    <h2>Productos</h2>
    {{#each productos}}
        <div>
            <h3>{{this.title}}</h3>
            <p>{{this.description}}</p>
            <p>{{this.category}}</p>
            <p>{{this.stock}}</p>
        </div>
    {{/each}}
</div>